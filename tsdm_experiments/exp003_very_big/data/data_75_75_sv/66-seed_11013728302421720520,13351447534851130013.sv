// Seed: 11013728302421720520,13351447534851130013

module elfshb (output trior logic [3:1] tuc [4:1], output real bsfbaoge, input wor logic [2:0][0:2][4:3] a [4:3][3:4], input int lnizsnuyg [4:2]);
  
  xnor ilqk(witclrev, xxzpegvsj, bsfbaoge);
  // warning: implicit conversion of port connection truncates from 64 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   real bsfbaoge -> logic bsfbaoge
  
  xnor gdl(rg, z, floxhuy);
  
  
  // Single-driven assigns
  assign bsfbaoge = bsfbaoge;
  
  // Multi-driven assigns
  assign xxzpegvsj = 'b001;
  assign rg = z;
endmodule: elfshb

module vueuiuyhxl (input wor logic [1:1][4:2] lnnu [1:0][2:3], input tri1 logic mocba, input uwire logic [3:1][3:0][3:3] boet [2:0][1:0]);
  // Unpacked net declarations
  int pupl [4:2];
  trior logic [3:1] lueutb [4:1];
  int jnbtnqm [4:2];
  wor logic [2:0][0:2][4:3] wdwlghmd [4:3][3:4];
  trior logic [3:1] cdivpsqm [4:1];
  
  elfshb pazflh(.tuc(cdivpsqm), .bsfbaoge(mocba), .a(wdwlghmd), .lnizsnuyg(jnbtnqm));
  // warning: implicit conversion of port connection expands from 1 to 64 bits
  // warning: implicit conversion changes signedness from unsigned to signed
  //   tri1 logic mocba -> real bsfbaoge
  
  elfshb szsty(.tuc(lueutb), .bsfbaoge(jmtz), .a(wdwlghmd), .lnizsnuyg(pupl));
  // warning: implicit conversion of port connection expands from 1 to 64 bits
  // warning: implicit conversion changes signedness from unsigned to signed
  //   wire logic jmtz -> real bsfbaoge
  
  xor hydkau(pbmp, jmtz, pbmp);
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign mocba = mocba;
  assign pbmp = mocba;
  assign lnnu = lnnu;
endmodule: vueuiuyhxl

module amep ();
  
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
endmodule: amep

module tdmxwx (output shortreal ucvp, inout tri logic [3:1][2:4][3:3] rotngqjjt [0:0][1:0][4:3][4:3], inout wor logic raysb);
  
  xnor dgybavwmv(ucvp, ucvp, ucvp);
  // warning: implicit conversion of port connection truncates from 32 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   shortreal ucvp -> logic ucvp
  //
  // warning: implicit conversion of port connection truncates from 32 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   shortreal ucvp -> logic ucvp
  //
  // warning: implicit conversion of port connection truncates from 32 to 1 bits
  // warning: implicit conversion changes signedness from signed to unsigned
  //   shortreal ucvp -> logic ucvp
  
  
  // Single-driven assigns
  
  // Multi-driven assigns
  assign raysb = 'bzx01;
endmodule: tdmxwx



// Seed after: 16705043013565769529,13351447534851130013
