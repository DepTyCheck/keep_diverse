-- Seed: 11902956041639736907,14997608447907677547

library ieee;
use ieee.std_logic_1164.all;

entity eipzvc is
  port (asmym : linkage time; h : out real; o : linkage std_logic; hvxozzeie : in character);
end eipzvc;



architecture qpje of eipzvc is
  
begin
  
end qpje;



entity vcyi is
  port (ykxtxf : buffer integer; ykkxu : linkage real);
end vcyi;

library ieee;
use ieee.std_logic_1164.all;

architecture gktmntoo of vcyi is
  signal e : character;
  signal ejq : std_logic;
  signal w : real;
  signal gwlyuyrgx : real;
  signal vn : std_logic;
  signal ywnvorifq : real;
  signal ftcc : character;
  signal mzoszqfh : std_logic;
  signal ehwwl : real;
  signal wes : time;
begin
  li : entity work.eipzvc
    port map (asmym => wes, h => ehwwl, o => mzoszqfh, hvxozzeie => ftcc);
  sqrs : entity work.eipzvc
    port map (asmym => wes, h => ywnvorifq, o => vn, hvxozzeie => ftcc);
  zrvqrsn : entity work.eipzvc
    port map (asmym => wes, h => gwlyuyrgx, o => mzoszqfh, hvxozzeie => ftcc);
  dhsjmkzdm : entity work.eipzvc
    port map (asmym => wes, h => w, o => ejq, hvxozzeie => e);
end gktmntoo;



-- Seed after: 2472794173297625700,14997608447907677547
