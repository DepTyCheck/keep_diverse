-- Seed: 7527978801495563724,14997608447907677547



entity c is
  port (u : inout real; t : out real; sdeiivvu : inout integer);
end c;



architecture tvfpucntrl of c is
  
begin
  
end tvfpucntrl;

library ieee;
use ieee.std_logic_1164.all;

entity rbo is
  port (y : buffer real; ytgad : out integer; idymew : inout std_logic; cqprdryz : linkage time);
end rbo;



architecture h of rbo is
  signal lvxb : real;
  signal yvnq : integer;
  signal kkv : real;
  signal mnxor : real;
  signal f : integer;
  signal mxs : real;
  signal lcdpfvskfd : real;
  signal kmmeqc : integer;
  signal xtiipfrt : real;
  signal dcskzuw : real;
begin
  ixal : entity work.c
    port map (u => dcskzuw, t => xtiipfrt, sdeiivvu => kmmeqc);
  sgwmaxodu : entity work.c
    port map (u => lcdpfvskfd, t => mxs, sdeiivvu => f);
  qoiv : entity work.c
    port map (u => mnxor, t => kkv, sdeiivvu => yvnq);
  a : entity work.c
    port map (u => y, t => lvxb, sdeiivvu => ytgad);
end h;



entity agjps is
  port (fcgragls : inout integer);
end agjps;



architecture ifeqatoz of agjps is
  signal i : real;
  signal szdeymrqrw : real;
  signal gpirukdso : integer;
  signal lralt : real;
  signal cuwnqzd : real;
  signal tgppagpuj : integer;
  signal ppzs : real;
  signal xokvb : real;
begin
  ojawgwnje : entity work.c
    port map (u => xokvb, t => ppzs, sdeiivvu => tgppagpuj);
  e : entity work.c
    port map (u => cuwnqzd, t => lralt, sdeiivvu => gpirukdso);
  yfdq : entity work.c
    port map (u => szdeymrqrw, t => i, sdeiivvu => fcgragls);
end ifeqatoz;



entity zi is
  port (orildkb : linkage real; fxmnjwq : linkage boolean; ig : inout integer; botooqyqv : in real);
end zi;



architecture lyeqckdrb of zi is
  
begin
  zkkloajmw : entity work.agjps
    port map (fcgragls => ig);
end lyeqckdrb;



-- Seed after: 10478996519913918006,14997608447907677547
